const { User } = require('../models');

const resolvers = {
  Query: {
    me: async (parent, args, context) => {
    }
  },

  Mutation: {
    login: async () => {

    },
    addUser: async () => {

    },
    saveBook: async () => {

    },
    removeBook: async () => {

    },
  }
}